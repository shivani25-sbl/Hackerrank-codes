#include<stdio.h>
int main(){
    int n , bin=0 ;
    print("Enter number :");
   int temp = n;
   int place =1;
   while (temp != 0){
    int rem = temp%16;
    bin = bin + rem * place;
    temp = temp/16;
    place *= 10;
   }
   printf("Hexadecimal of %d is %d" , n , bin);
    return 0;
}
