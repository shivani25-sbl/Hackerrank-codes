#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main(){
    int N,i ;
       scanf("%d" , &N);
        int arr[N];
        for( i=0 ; i<=N ; i++){
            scanf("%d" , &arr[i]);
}
    for( i=0 ; i<N ; i++){
        printf("%d " , arr[i]);
    }
    return 0;
}
