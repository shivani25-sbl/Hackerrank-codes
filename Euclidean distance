#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
int x1 = 0,y1 = 0,x2 = 3,y2 = 4;
scanf("%d %d %d %d",&x1, y1, x2,y2);
float distance;
scanf("%f",&distance);
distance = sqrt((x2 x1)(x2x1) + (y2y1) (y2-yl));
printf("%.2f", distance);
return 0;
}
