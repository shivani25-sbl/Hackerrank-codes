#include <stdio.h>

int main() {
    int N;
    scanf("%d", &N);

    int arr[100];
    for(int i = 0; i < N; i++) {
        scanf("%d", &arr[i]);
    }

    int X;
    scanf("%d", &X);
    for(int i = N; i > 0; i--) {
        arr[i] = arr[i - 1];
    }
    arr[0] = X;

    for(int i = 0; i <= N; i++) {
        printf("%d", arr[i]);
        if(i != N) {
            printf(" ");
        }
    }

    return 0;
}
